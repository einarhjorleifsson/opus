------------------------------------------------------------------------

# opus — ICES DATRAS Data Dictionary and Known-Issues Registry

**Status:** Active (2026–onward). **Version:** 0.2.0+

------------------------------------------------------------------------

## The YAML is the deliverable

opus produces **two YAML specifications**:

1. **`DATRAS-data-dict.yaml`** — Descriptive specification of Tier 1 (HH, HL, CA, LT). Documents what the archive **actually contains** — includes all observed enum values (both icesVocab codes and undocumented variants), with legacy field names for backward compatibility. Use as reference for understanding real-world patterns.

2. **`DATRAS-known-issues.yaml`** — Registry of metadata gaps between official ICES specs and real submission patterns. Tracks type mismatches, undocumented codes, incomplete vocabularies, and their escalation status with ICES.

The R package (`opus`) provides **thin tooling** around these YAML files: validation functions, generation scripts, and documentation. The YAML itself is the reference — portable, language-agnostic, and suitable for any tool.

------------------------------------------------------------------------

## What the project does

1. **Consolidates scattered knowledge** — WSDL, icesVocab, Technical Reference, real submissions — into one authoritative YAML
2. **Documents reality vs. spec** — When real data diverges from official documentation, capture it (not as error, but as known issue)
3. **Enables pre-submission validation** — Submitters can validate locally before uploading to ICES
4. **Escalates problems upstream** — Known-issues registry feeds into imbus/ICES dialogue

See `vignettes/why-opus.qmd` for context and philosophy.

------------------------------------------------------------------------

## Working Principles

**1. Real data is ground truth.** When WSDL/icesVocab/specs conflict with live submissions, trust the submissions; file the divergence as a known issue.

**2. Consolidate scattered knowledge.** WSDL, icesVocab, Technical Reference, real submissions — bring together into one machine-readable place.

**3. Metadata-centric, not domain logic.** opus ships YAML specs + metadata validation and curation tooling (21 R functions). These functions work *on* the specification and data patterns, not *on* domain questions. No contextual QC (e.g., "door spread constraints vs. depth"), no statistical analysis, no derived products. That computational work belongs in obus/imbus.

**4. Don't guess; document.** Every range/constraint/enum needs evidence: real data, WSDL, or icesVocab. Borderline calls get flagged in `details:` for expert review.

**5. Separate concerns: specs ≠ QC.** Known-issues registry documents schema gaps (upstream at ICES). Data-quality problems (wrong values) belong downstream in obus/imbus. opus surfaces whether data violates the *documented* spec; imbus solves whether it's *valid* in context.

**6. Shared fields stay consistent.** HH/HL/CA/LT repeat field names (same facts). Type, units, range must match byte-for-byte across tables.

**7. Adapt when needed.** Principles are Magna Carta, not law. Revisable when practice demands.

**8. Verify empirically.** A claim only counts as checked once confirmed against live, primary data — the real archive, the real service response — not by re-reading a document, a citation, or your own earlier note.

**9. Audit exhaustively.** Cover every relevant variant before concluding something is missing or absent — legacy name and current name, every prefix candidate — not just the first one that resolves. A confirmed null result is only meaningful if it's a null result for the complete set of things worth checking, not a partial one. See the icesVocab field-name dependency note below for the concrete case that forced this rule into existence (2026-08-08): each individual check that day was genuinely empirical (principle 8) yet the sweep still missed the answer, because it wasn't exhaustive (principle 9) — the two failures are independent, and both are needed to actually verify something.

------------------------------------------------------------------------

## Task

**opus feeds imbus WP2 (reference specification).** Deliver the most accurate DATRAS data-dict YAML, verified against real submissions. Documents the schema as actually submitted, flags upstream divergences (WSDL/icesVocab gaps), and escalates them to ICES. This is the work.

**opus does NOT:**
- Build QC infrastructure (imbus WPX handles complex contextual validation — e.g., door spread constraints vs depth)
- Compute or transform data
- Analyze or report on data quality independently
- Solve multivariate co-parameter constraints (beyond data-dict's scope)

**Thin wrappers only:** Functions are OK if they're glue around external tools (e.g., `validate_with_datadict()` wrapping the data-dict CLI for basic spec/metadata/data validation). Computation is not — that belongs downstream.

**Why we monitor data-dict:** data-dict's three-level validation (spec/metadata/data) covers the basic checks; R-package (when available) could remove friction for users who want that validation. But imbus's **real QC work** — complex contextual rules like "door spread limits depend on depth" — is beyond data-dict's scope and belongs to imbus's own QC workpackage. We track data-dict to prevent reinventing spec validation, but set realistic expectations about what it can deliver. See [[data_dict_trajectory]] for roadmap.

### Decision Gate: New Work

Before starting work on opus, ask:
1. Does this improve the YAML specification (types, constraints, values, known issues)?
2. If adding code: does it work *on* the specification (validation, curation, metadata utilities) or work *with* the specification (domain QC, contextual constraints, data transformation)?
   - *On* the spec → belongs in opus (metadata-centric per Principle 3)
   - *With* the spec → belongs in obus/imbus (domain logic)
3. Will this function help data submitters validate locally or help opus maintainers curate specs?
4. If uncertain: flag for explicit decision before proceeding.

------------------------------------------------------------------------

## Scope

**Tier 1: DATRAS Exchange Data (raw submissions)**
- **HH** (Haul Information): Primary exchange table
- **HL** (Length Frequency): Primary exchange table
- **CA** (Catch at Age): Primary exchange table
- **LT** (Litter Assessment): Hybrid—some novel observations + lookup/join fields to HH. Attached to Tier 1 but not purely exchange data.

**Tier 2: DATRAS Derived Products (computed from Tier 1, future)**
- **CPUEL, CPUEA** (Catch Per Unit Effort): Computed from HH/HL with species name lookup
- **FL** (Fishing Effort): Derived from HH
- **IDX** (Survey Index): Derived product
- These are processed outputs, not raw submissions

**Tier 3: obus Derived Products (future)**
- Contracts, aggregations, and domain-specific derivatives built on top of opus specs

------------------------------------------------------------------------

## Format: data-dict.yaml v0.1.0

- Uses relationships, constraints, definitions, glossary, todo (added 2026-08-16 -- see that date's entry below); not yet assertions, for which no evidence-backed candidate has been identified (Working Principle 4 -- not guessed into existence just because the feature exists)
- One file per tier
- Schema closed: only standard keys allowed

------------------------------------------------------------------------

## Known-Issues Registry

`inst/DATRAS-known-issues.yaml`: Escalation log for ICES Datacenter. Documents where official specs diverge from actual submissions. Schema/vocab problems only (not data-quality issues).

------------------------------------------------------------------------

## Data Sources

opus consolidates metadata from three ICES web services:

1. **WSDL (Web Services Definition Language)**
   - Source: `https://datras.ices.dk/WebServices/DATRASWebService.asmx`
   - Provides: Field types (string/int/decimal) for each operation
   - Authority: Primary—describes what the service actually returns
   - Scope: DATRAS-specific (HH, HL, CA, LT operations only)

2. **getDatrasFieldList API**
   - Source: `https://datras.ices.dk/WebServices/DATRASWebService.asmx/getDatrasFieldList`
   - Provides: Field name mappings (old → new), descriptions
   - Authority: Secondary—derived from WSDL and internal DATRAS schema, and
     **confirmed unreliable** (2026-08-06): covers only 22 of LT's 58 real
     fields, wrongly claims 3 LT fields were never renamed, documents a
     `RecordHeader` field LT doesn't have, and pairs CA's `IndividualAge`
     with an old-name (`AgeRings`) that doesn't exist. Filed with ICES —
     see `data-raw/ICES_ISSUE_REPORT.md`.
   - Scope: DATRAS-specific (no other ICES products)
   - Note: DataFormat field can diverge from WSDL; opus sources types from WSDL directly

3. **icesVocab (ICES Vocabularies)**
   - Source: `https://vocab.ices.dk/services/api/`
   - Provides: Code definitions and meanings (e.g., Gear codes, species validation)
   - Authority: Cross-domain reference—used by multiple ICES data systems
   - Scope: Not DATRAS-exclusive; opus filters to vocabularies applicable to DATRAS fields
   - Note: Provides code semantics only, not structural types

opus unifies these three sources into a single YAML specification, making inconsistencies visible and escalatable to ICES.

**No R-package dependency on either `icesDatras` or `icesVocab`** (removed
2026-08-06): opus calls these two web services directly (`R/vocab.R`,
`R/field_names.R`'s `op_datras_field_list()`), verifying every claim against
at least two independent live sources rather than trusting either service's
metadata blindly — this is how the getDatrasFieldList errors above were
caught.

**A note on `icesDatras` citations in this document and elsewhere in opus
(corrected 2026-08-09):** earlier entries below describe `icesDatras` as
silently hand-patching `getDatrasFieldList()` (a fabricated `lt_extra`
table for LT, explicit overrides, etc.). Checked directly against the
current official `ices-tools-prod/icesDatras` on GitHub: its
`getDatrasFieldList()` is four lines -- fetch the live endpoint, parse the
XML, return it. No patches, no overrides, no `data-raw/` directory at all.
That patch-narrative describes a personal development fork
(`einarhjorleifsson/icesDatras`, branch `einar_dev/integration`) that was
installed locally at the time, not the official package. One specific claim
this fed was actively wrong, not just imprecisely attributed: `getDatrasFieldList()`'s CA `IndividualAge`/`AgeRings` row (Issue 4,
`data-raw/ICES_ISSUE_REPORT.md`) was described as something the icesDatras
patch "still trusts" -- but the fork's own patch had already fixed that
exact row three weeks before the report text was written. Every citation
below and across the rest of opus (comments, roxygen docs, the ICES issue
report, and the -9-sentinel claim baked into `inst/DATRAS-data-dict.yaml`)
was re-checked against the current official repo and corrected 2026-08-09:
retracted where the citation described something that only exists on the
fork (the hand-patch narrative, `data-raw/build_datras_schema.R` and its
siblings, none of which exist upstream at all), left in place and
re-pointed at the right function where the underlying fact turned out to
be genuinely true of the official package too (the -9 scrub, real and
current in `parseDatras()`, just previously mis-cited as
`applyDatrasTypeSchema()`, which doesn't do it).

## Implementation

**User-facing exported functions** (21 total):

*Specification validation, parquet exploration, and drafting* (`R/validation.R` -- all of them; despite the name, `op_describe_parquet()`/`op_draft_from_parquet()` live here too, not in a separate file):
- `op_validate_spec(dict_path, json)` — Check YAML spec conformation (uses data-dict CLI); `json=TRUE` also returns the structured report (`$report`)
- `op_validate_meta(data_path, table, dict_path)` — Validate column names/types
- `op_validate_data(data_path, table, dict_path)` — Validate values vs constraints
- `op_validate_full(data_path, table, dict_path)` — Run all three checks
- `op_inspect_parquet(parquet_path)` — See what data-dict CLI sees
- `op_flag_violations(data_path, table, dict_path)` — Flag EVERY row violating required/enum/range constraints, exhaustively. Complements, not superseded by, the CLI's own report (`op_validate_meta()`/`op_validate_data()`'s `$result`, `op_validate_spec(json=TRUE)`'s `$report`): the report is capped at the first 5 rows/problem (no CLI flag raises this — see `site/report.md#counting-and-capping` in the data-dict repo), so this function still exists specifically for exhaustive marking.
- `op_validation_problems(report)` — Flatten any of the above reports' `problems` into a data frame (pure reshaping, no new checks; added 2026-08-16)
- `op_describe_parquet(parquet_path)` — Describe parquet structure and statistics
- `op_draft_from_parquet(parquet_paths, output_path)` — Generate skeleton YAML from parquet data
- `op_export_spec(dict_path)` — Export a fully-resolved dictionary as JSON (wraps data-dict CLI's `export-spec`)
- `op_export_data(dict_path)` — Export a dictionary with per-column data profiles as JSON (wraps data-dict CLI's `export-data`)
- `op_render_spec(dict_path, output, data_dir)` — Render a dictionary as a self-contained HTML page (wraps data-dict CLI's `render`); on-demand tool only, nothing shipped/committed (2026-08-16 documentation-architecture decision, see that date's entry below). `data_dir` (e.g. `.datras/`) profiles against a full local archive instead of the shipped `inst/*.parquet` samples.

*ICES Vocabulary utilities* (`R/vocab.R`):
- `op_vocab_get_types()` — Get all ICES vocabulary code-types with prefix metadata
- `op_vocab_resolve_key(field_name, types)` — Find candidate vocabulary keys for a field, given the full domain table from `op_vocab_get_types()`
- `op_vocab_get_codes(vocab_key)` — Fetch code:description pairs for a vocabulary
- `op_vocab_first_usable(vocab_keys)` — Select first non-empty vocabulary from candidates
- `op_vocab_resolve_datras_key(table, field)` — Look up opus's own audited vocab-key proposal for a Tier 1 field from the pre-computed correction table (`inst/DATRAS-vocab-correction.csv`)

*Field name utilities* (`R/field_names.R`):
- `op_legacy_field_name(details)` — Extract legacy (old ICES) field name from YAML details
- `op_field_name_map(dict, table_name=NULL)` — Build legacy→new field name mapping table
- `op_datras_field_list(tables)` — Derive verified Tier 1 old-name→new-name mappings directly from ICES's live services (replaces `icesDatras::getDatrasFieldList()`); tiers each mapping as `confirmed` / `cross_table_confirmed` / `no_evidence` rather than trusting ICES's field-list metadata blindly. See its own roxygen docs for the six confirmed ICES-side errors this caught.
- `op_datras_rename_crosswalk()` — The legacy→curated rename table `spec_03_translate_new_names.R` actually applies (wraps `op_datras_field_list()`, with the one known correction and a general collision guard, not a hardcoded special case)

These wrappers enable the curation loop: build YAML → validate against real data → identify issues → refine YAML. The **descriptive YAML** (DATRAS-data-dict.yaml) is the reference for what exists in practice, including both icesVocab-defined codes and undocumented variants observed in submissions.

**Development-only functions** (`R/` — not exported):
- `op_minimal_yaml()` — Generate minimal type-only YAML from WSDL (Phase 1 seed)
- `op_enrich_stage2_yaml()` — Enrich with icesVocab codes (Phase 2)
- `op_apply_curated_spec()` — Apply field renames and type refinements (Phase 3)
- `op_build_final_yaml()` — Merge curated + enriched YAML
- `op_check_type_mismatch()` — Audit types across WSDL/curated/real data
- `op_audit_yaml_phase2_mismatch()` — Compare versions during curation

These are internal to the three-phase bootstrap workflow; see [[project_bootstrap_yaml_workflow]] for rationale.

**Source scripts** (`data-raw/`, spec-building pipeline; renamed to a scoped `spec_0N_` prefix 2026-08-08; restructured to be legacy-name-primary and gained a dedicated translate step 2026-08-09, see that date's entry below):
- `spec_00_operation_types.R` — WSDL operation-page crawler (`.fetch_datras_operation_fields()`'s data source); sourced by `spec_01_seed_dict.R`
- `spec_01_seed_dict.R` — pull specs from live WSDL + getDatrasFieldList + icesVocab (no corrections); keyed by ICES's own legacy (real, on-the-wire) field names
- `spec_02_curate_dict.R` — apply hand-written corrections, enrich enums, keyed by legacy field names throughout; writes `inst/DATRAS-data-dict-legacy.yaml`
- `spec_03_translate_new_names.R` — pure rename, legacy → opus's curated names, via `op_datras_rename_crosswalk()` (`R/field_names.R`); writes `inst/DATRAS-data-dict.yaml`, the only step that introduces new names at all

`spec_04_dict_to_qmd.R` (the hand-rolled Quarto-table generator this pipeline used to end with) was removed 2026-08-16, in favor of data-dict's own native `render` command (`op_render_spec()`, `R/validation.R`) — a self-contained HTML page with a relationship diagram, searchable index, and live data profiling, none of which the hand-rolled generator ever did (it also never rendered `relationships` at all). `DATRAS-data-dict.qmd`/`DATRAS-data-dict-legacy.qmd`/`DATRAS-known-issues.qmd` (its generated output) removed with it. `known-issues.yaml` has no `render` equivalent — it isn't a real data-dict.yaml document (confirmed: `render` rejects it, "Unknown property 'sentinels'") — and stays plain YAML for now, no generated page.

There is a second, separate pipeline (`data-raw/archive_0N_*`) that downloads and converts the real DATRAS archive rather than building the spec; see the 2026-08-08 data-raw reorg entry below for both pipelines and what else was removed.

Historical note: `data-raw/ices_api.R` (`op_fetch_datras_field_list()`/`op_fetch_vocab_codes()`, direct ICES API dev wrappers) was removed 2026-08-08 — dead code with zero callers, properly superseded by the exported `op_datras_field_list()` (`R/field_names.R`) and `op_vocab_get_codes()` (`R/vocab.R`).

------------------------------------------------------------------------

## Open Items

**Scope clarity: opus is WP2 (reference spec); WP3 owns operational QC on vessels.** opus's task is the reference YAML specification and processed data products for imbus's WP2. imbus's **QC work** (on-vessel quality control, including domain violations like "door spread constraints depend on depth") lives in **WP3** (Marine Institute, Stokes) — a separate workpackage that requires operational QC tooling, not data-dictionary work.

**data-dict's realistic role:** data-dict covers three validation levels (spec/metadata/data) — sufficient for catching schema violations and basic constraint checking. But imbus's **operational QC** (multivariate co-parameter rules, on-vessel validation logic, domain-specific limits) is beyond data-dict's scope. opus **optionally** wraps data-dict for basic validation (if/when R-package matures); but the heavy QC lifting belongs to WP3's operational tools.

**Why we monitor data-dict:** Prevents reinventing spec validation; clarifies boundaries. When their R-package ships, opus can offer it as a user convenience (via thin wrappers). But WP3 should not expect data-dict to solve their operational QC problem — that's a separate engineering effort. Monitoring helps opus stay proportionate and avoid scope creep into work that's not ours. See [[data_dict_trajectory]] and [[imbus_structure]] for roadmaps.

**icesVocab field-name dependency:** A critical architectural discovery (2026-08-02): icesVocab code lookups are **dependent on field names**, and those field names are both old (legacy ICES names) and new (current names), sometimes pointing to **different vocabularies**. Example: `Sex` has `TS_Sex`/`AC_Sex` (vocab, ambiguous between trawl and acoustic domains), but `IndividualSex` does not (both re-verified live 2026-08-08); `GearEx` has `TS_GearEx` (**13** codes) while `GearExceptions` has `AC_GearExceptions` (**1** code) -- corrected 2026-08-08, these two figures were previously swapped/wrong in this note. Solution: document legacy field names in YAML details field using standardized prefix (`Legacy field name: {OldName}`), and provide dual-lookup wrappers in R/vocab.R + R/field_names.R for downstream validation code. See `vignettes/articles/technical-notes.md` for full discovery notes and implementation details.

**Corollary, learned the hard way (2026-08-08):** knowing this principle abstractly does not stop you from tripping over it. Checking `HH.HaulValidity` and `HL.SpeciesValidity` against icesVocab using their *current* names resolved to `AC_HaulValidity` (2 codes) and `AC_SpeciesValidity` (2 codes) -- producing a false "5 of 7 real values missing from vocab" / "8 of 10 missing" finding, reported as fact before being challenged. Re-resolving against their *legacy* names (`HaulVal` -> `TS_HaulVal`, `SpecVal` -> `TS_SpecVal`) showed a perfect match both directions, both fields -- no gap at all. **Before concluding any real value is missing from icesVocab, resolve against both the legacy name and the current name and check both candidates -- resolving only the current name is not enough, even for someone who already knows this note exists.**

**Full systematic audit, same day, all 52 Tier 1 enum fields, both names checked for every one:** icesVocab is empirically legacy-name-keyed, cleanly, no exceptions. Of the 21 renamed enum fields where any vocab match exists at all, **100% (21/21)** resolve via the legacy name; **0/21** resolve via the current name. The legacy-name-vs-current-name split isn't a pitfall confined to a couple of known cases -- it's the rule. Overall: 46 fields get a full bidirectional match on their best candidate, 6 have no icesVocab entry under *either* name (`RecordHeader`/`RecordType` and CA's three Sampling Flag fields -- legitimately no vocab exists, not a coverage gap), and **zero genuine gaps** once resolved correctly. Cross-checked the yaml's own `details:` text too, everywhere it names a specific vocab key inline: every citation agrees with this fresh live re-derivation -- the existing Stage 3 enrichment work was not silently built on the wrong name. Practical upshot: when resolving a DATRAS field against icesVocab, resolve the *legacy* name first; the current name is very unlikely to be the one that matters.

**Enum coverage audit (2026-08-02):** Systematically audited all 52 enum fields in Tier 1 against icesVocab. Findings: 35 fields (67%) have full coverage; 17 fields (33%) have no icesVocab match (system/metadata fields); 4 fields have incomplete coverage (HaulValidity 29%, SpeciesValidity 20%, GearExceptions 8%, LengthCode 43%); 1 field has empty vocab despite key existing (AgePreparationMethod); 12 fields exist under both TS_ and AC_ domain prefixes with ambiguity. Outputs: enum_field_inventory.csv, enum_enrichment_analysis.md. Key insight: incompleteness is **systemic** — not random gaps but patterns suggesting domain-specific extensions not in centralized icesVocab.

**Type source authority (2026-08-02):** Discovered three ICES metadata sources claim to define field types and **they diverge**. WSDL is authoritative (auto-generated from server code). getDatrasFieldList is secondary (hand-maintained table). Five confirmed divergences: Year fields (WSDL=int, API=char; real archive=int), Distance (WSDL=int, API=float; real archive=int). opus seeds from WSDL only. Documented in known-issues #10 (datras_field_list_type_divergence) as institutional accountability marker — ICES must align getDatrasFieldList with WSDL and real data behavior.

**What opus really is (2026-08-02 realization):** Not a data reformatting tool. **opus is institutional data governance audit infrastructure.** Systematically exposes where ICES's three metadata sources (WSDL, getDatrasFieldList, icesVocab) diverge from each other and from real data. Known-issues registry becomes accountability log: "Here's evidence this is broken, here's the impact, here's how to fix it." This reconciliation work is the actual deliverable — YAML specs are the vehicle, but exposure of governance gaps is the value.

**Dependency removal + getDatrasFieldList audit (2026-08-06):** Removed opus's R-package dependency on both `icesDatras` and `icesVocab` (direct web-service calls instead, verified byte-identical for icesVocab's two endpoints). Building the replacement (`op_datras_field_list()`, see `R/field_names.R`) required cross-verifying ICES's live `getDatrasFieldList` metadata against each operation's own ASMX response and the real archive, which surfaced 6 confirmed ICES-side errors (LT coverage gap, 3 wrong LT renames, a phantom LT `RecordHeader` field, CA's unverifiable `IndividualAge`/`AgeRings` row, missing `DateofCalculation`/`Valid_Aphia` entries, and LT's `Depth`/`BottomDepth` data duplication) — filed as `data-raw/ICES_ISSUE_REPORT.md` (moved from `inst/` 2026-08-08), not yet sent to ICES. Corrected opus's own spec accordingly: `CA.IndividualAge` reverted to `CA.Age` (the rename rested on an unverifiable metadata row), LT's ~23 shared fields renamed via verified cross-table inference (matching what a personal development fork of `icesDatras` had already worked out for these same fields, but sourced and documented here rather than just inherited -- see the 2026-08-09 note above; the official `ices-tools-prod/icesDatras` carries no such patch at all). **Still open:** the earlier same-day session (before this dependency work) produced several documents (`PHASE2_ISSUES_FOR_ICES.md`, `PHASE3_ROADMAP.md`, `DATRAS_PHASE2_ORIGINAL_NAMES.yaml`, `ICESVOCAB_MAPPING_AUDIT.md` and related CSVs) containing fabricated row counts and misattributed sources, discovered when this session's rigor caught a specific bad claim — their fate (delete vs. mark superseded) is undecided; do not treat their contents as reliable.

**Legacy/new parquet split + full ground-truthing (2026-08-08):** `.datras/parquet/LT` had no partitioned directory (unlike HH/HL/CA) because LT's XML predates the manifest-tracked downloader; backfilled via `data-raw/archive_05_backfill_lt_partitions.R` (204 files, 75,310 rows — matches the pre-existing consolidated total exactly). That backfill surfaced two latent bugs in `data-raw/archive_04_parse_phase2.R` itself (LT wrongly required a `RecordType` field it doesn't have; an all-empty XML file crashed instead of being recorded as `EMPTY_RECORDS`), fixed in both scripts. Then split the consolidated legacy archive from the curated spec: `.datras/{HH,HL,CA,LT}_legacy.parquet` (moved from `.datras/parquet/{table}.parquet`, current ICES field names, full archive) sit alongside new `.datras/{HH,HL,CA,LT}.parquet` (same data, opus-curated names — column rename is the *only* difference) and `.datras/DATRAS-data-dict-legacy.yaml` (pure name-swap of `inst/DATRAS-data-dict.yaml`, nothing else touched), all built by `data-raw/archive_06_split_legacy_new.R`. Along the way, found `data-raw/seed/DATRAS-exchange-name-history-seed.csv` is stale for LT (missing the ~23-field cross-table-inference correction above — confirmed by running it, not assumed), so the crosswalk is derived from the yaml's own `Legacy field name:` annotations instead — via the real `op_legacy_field_name()` (`R/field_names.R`, `source()`d directly, matching the precedent in `data-raw/spec_02_curate_dict.R`), not a second copy of its regex; a second-look review caught that the script had initially duplicated the regex inline, confirmed it agreed with the real function on all 190 columns, then switched to calling the real one to remove the drift risk — asserted against the real legacy parquet's actual columns before anything is written. Also fixed a bad annotation this surfaced: LT's `BottomDepth` details wrongly said `Legacy field name: Depth`; corrected at the source in `inst/DATRAS-data-dict.yaml` (BottomDepth is its own legacy field, not a rename — see Issue 6). Final step: independently re-verified the full crosswalk for all four tables directly against raw XML (not the parquet, not this file, not memory) — at least one non-empty sample per survey, all 28 surveys per table — zero discrepancies. See the 2026-08-08 addendum in `data-raw/ICES_ISSUE_REPORT.md` for the LT-specific re-confirmation, and its Issues 2–3 (reformatted 2026-08-08 to lead with legacy names, per house style: issue reports to ICES are framed in ICES's own current vocabulary; opus's own legacy→new mapping decisions are a separate entity, recorded in the yaml, not in the issue report). **Second-look review, same day:** re-verified column *values* (not just names) survived the rename correctly — sampled 50 random rows per table, every column, legacy value equals the value under its renamed column, all four tables — and confirmed the legacy yaml's 13-line-shorter line count against the source is pure `yaml::write_yaml()` re-wrapping, not lost content (every non-`name` field across every column compared programmatically, zero diffs, glossary and top-level metadata included). **The BottomDepth fix above only patched the symptom, not the cause — found and fixed properly during this same review.** `data-raw/spec_02_curate_dict.R`'s `add_legacy_field_names()` independently rebuilds its own old-name lookup from `op_datras_field_list()` and never inherited the LT `Depth`/`BottomDepth` exception already applied to the separate `lt_renames` step a few dozen lines above it (~line 166) — so `op_datras_field_list()`'s cross-table inference (correctly: "HH confirms Depth→BottomDepth, LT also has a Depth column, so infer the same rename for LT") wrongly tags LT's real, separate `BottomDepth` column with that inference, with nothing downstream aware of the LT-specific duplication. Confirmed by calling the live function directly: exactly one row matches (`LT, Depth, BottomDepth, cross_table_confirmed`), and the fix (excluding it from `add_legacy_field_names()`'s lookup the same way `lt_renames` already excludes it) removes only that row, leaving all other real LT renames (`Ship`→`Platform` etc.) untouched. Without this, the next full regeneration of `inst/DATRAS-data-dict.yaml` via this script would have silently reintroduced the exact bug just fixed. Also regenerated `vignettes/articles/DATRAS-data-dict.qmd` from the corrected yaml (2-line diff, exactly the BottomDepth/Depth text) and fixed the same stale `inst/ICES_ISSUE_REPORT_20260806.md` path in two more places inside `spec_02_curate_dict.R` itself and one live TODO.md checklist item (left the file's own "Latest" historical narration line untouched, since it's an accurate statement of what was true on 2026-08-06).

**data-raw/ audit and reorg (2026-08-08, same day):** User asked for a deep review of every `data-raw/` script for orphans, duplication, and naming. Traced every `source()` call and `Rscript data-raw/X` invocation across the whole repo (not filenames/headers alone) to build the real dependency graph, confirming two independent pipelines by actual git history: **spec-building** (`spec_00_operation_types.R` → `spec_01_seed_dict.R` → `spec_02_curate_dict.R` → `spec_03_dict_to_qmd.R`, first committed 2026-07-31, builds `inst/DATRAS-data-dict.yaml`) and **data-archival** (`archive_01_download_config.R`/`archive_02_download.R`/`archive_03_catalog.R` → `archive_04_parse_phase2.R` → `archive_05_backfill_lt_partitions.R` → `archive_06_split_legacy_new.R`, first committed 2026-08-05, downloads the real archive and consumes the spec pipeline's yaml). Both renamed from their original flat names (`DATASET_*`, `datras_*`) to this scoped `_0N_` prefix per pipeline — deliberately not one global sequence, since the two pipelines don't actually run in one order.

Removed 6 confirmed-dead scripts, each verified individually rather than assumed from naming: `datras_phase2_stage1.R` (read a spec file, `inst/DATRAS-types-minimal.R`, that doesn't exist — couldn't have run), `xml_to_parquet.R`/`.py` and `xml_to_csv.py` (standalone converters from an earlier iteration, zero callers anywhere, superseded by the inline parser now in `archive_04_parse_phase2.R`), `ices_api.R` (`op_fetch_datras_field_list()`/`op_fetch_vocab_codes()`, zero callers, superseded by the exported `op_datras_field_list()`/`op_vocab_get_codes()`), and `datras_vocabulary.R` (self-declared deprecated in its own header, confirmed zero functional callers — the handful of remaining mentions in `spec_02_curate_dict.R`'s `details:` text are accurate historical citations of what tool verified a specific field on 2026-07-29, deliberately left as-is rather than scrubbed). Found but did **not** remove a third, real duplicate: `archive_03_catalog.R::datras_get_field_list()` is a second live implementation of "fetch getDatrasFieldList," actually called by `archive_02_download.R` to hash the raw response for manifest provenance — a different purpose from the package's own verified `op_datras_field_list()`, not simply redundant.

Also found: `spec_00_operation_types.R`'s own comments reference `data-raw/build_datras_schema.R` and `data-raw/DATASET_RAW.R`, files that don't exist anywhere in opus — leftover narrative from wherever this file was originally written (its header says "built from the obus side"), even though its two functions are genuinely live (sourced by `spec_01_seed_dict.R`). Left as-is (out of scope to rewrite the historical narrative in a file already mid-rename), but worth knowing before trusting those specific comments. Moved the 9 untracked `*_retired.*` files (already `.gitignore`'d by pattern, confirmed via `git check-ignore` — never lost git history since they never had any) into `data-raw/retired/`, and deleted the untracked `data-raw/DATRAS-data-dict_files/` (a stray Quarto render-support directory, no content of value).

Every rename's cross-references were fixed by exhaustive repo-wide grep before and after, not assumed complete after the obvious spots: `source()` calls between the renamed scripts, each renamed file's own usage/help text, `R/field_names.R` + `R/op_minimal_yaml.R` (one of which had a live, executable `source("data-raw/datras_operation_types.R")` call that would have broken `op_minimal_yaml()` if missed) plus the man page that generates from it (regenerated via `devtools::document()`, not hand-edited), `vignettes/articles/technical-notes.md`, `TODO.md`, and this file — including a whole stale "ICES API wrappers" section here describing the just-deleted `ices_api.R` as if still current, and a "Source scripts" entry for `yaml_to_formatted_md.R`, a file already deleted in the *previous* (2026-08-07/08) session and never updated here. One gap the first sweep missed and a second pass caught: a comment inside `spec_02_curate_dict.R`, added earlier this same session, cited `split_legacy_and_new.R` by its pre-rename name — missed because the archive-pipeline and spec-pipeline rename passes were done separately without cross-checking each other's files. `inst/DATRAS-data-dict.yaml`'s own filename citations (an `origin:` field plus a couple of `details:` citations) were hand-patched to match what the corrected generator now produces, rather than re-running the full live-network curate pipeline for a pure filename-string change — then `vignettes/articles/DATRAS-data-dict.qmd`/`DATRAS-known-issues.qmd` regenerated from the corrected yaml and the diff confirmed to be exactly the expected strings, nothing else.

**XML->parquet type casting was silently broken; fixed by moving to WSDL-only, no yaml, no validation gates (2026-08-08/09):** User found this by actually using the data -- `open_dataset(".datras/parquet/CA")` then a `str_detect(GenSamp, ...)` filter crashed with a DuckDB schema-merge error ("failed to cast column GenSamp from type VARCHAR to INTEGER"). Root cause: `archive_04_parse_phase2.R`'s yaml-based `apply_rigid_types(df, rt, spec)` checked `rt %in% names(spec)`, but `spec` was the *whole* `inst/DATRAS-data-dict.yaml` object -- its top-level names are `tables`/`glossary`/`version`, never a table name. The check was always true, so the function silently returned `df` completely unchanged, for every call, every column, not just enum/string ones -- confirmed by testing the exact condition directly. Whatever type each column ended up as was purely whatever R's own `read.delim()` guessed from that one file's own values. Confirmed via a real per-file schema scan across all four tables: **16 CA columns and 8 HL columns** end up typed inconsistently across different files for the same column (e.g. `GenSamp`: `int32` in files where every row happens to be the `-9` sentinel, `string` in files with a real `Y`/`N` -- and the yaml already documented, since 2026-07-29, that GenSamp is a clean `Y`/`N` field; live WSDL independently confirms `string`, same for `StomSamp`/`ParSamp`). HH and LT happened to test clean, by luck of their data, not by correctness of the pipeline.

User's direction, confirmed correct for more than "sufficient" -- actually necessary: **this conversion stage should rely solely on live WSDL for types, never the curated yaml, and shouldn't even know the word "enum" exists.** WSDL's response is keyed by the raw XML's own legacy tag names, exactly what this stage parses (using the yaml, keyed by *current* names, would need an extra legacy/new cross-reference just to find the type -- another vector for the exact class of bug this whole session kept finding elsewhere). And enum-ness is a curation conclusion about a string field's value *domain*, reached by analyzing real archive data -- that analysis is downstream of this stage, so depending on it here is circular (you can't know a field is a clean two-value enum before you can reliably read the field at all). Built `data-raw/archive_00_wsdl_types.R`: `apply_wsdl_types(df, rt)`, casting straight from live WSDL (`string`/`int`/`decimal` -- verified operation names: `getHHdata`/`getHLdata`/`getCAdata`/`getLitterAssessmentOutput`), fetched once per table and cached. Validated against the two real files that exposed the bug (`CA_BITS_2016_Q1`, real `'N'` value; `CA_BITS_2010_Q1`, all-`-9` file that used to become `int32`): both now produce `character` consistently. Wired into both `archive_04_parse_phase2.R` and `archive_05_backfill_lt_partitions.R`; the yaml load (`opus_spec <- read_yaml(...)`) deleted from both entirely, confirmed via grep it had no other use in either file.

**Second, sharper correction from the same user, same session:** the "required fields" gate (reject a parsed record if e.g. `RecordType` is missing, different rules hardcoded per table) also doesn't belong at this stage, for the same reason as the yaml -- it's a completeness/validation judgment, not a type-casting one. Removed from both scripts. The stage's only remaining hard failures are genuine technical ones with no other option: XML that fails to parse at all, or has literally zero records to derive even a column list from. Reconciling column-set differences across files (if any) is now explicitly a downstream concern, not something this stage silently resolves by dropping data.

`archive_05_backfill_lt_partitions.R` (previously LT-only, because `.datras/manifest.tsv` has no LT rows) generalized to reprocess any/all of HH/HL/CA/LT: the manifest turned out to be stale for the *whole* bulk archive, not just LT (only 10 rows total, all one later NS-IBTS test run), so `archive_04`'s normal manifest-driven path can't bulk-reprocess any table. Filename kept as-is (avoided yet another rename pass); it now does more than LT.

**Session paused mid-reprocessing, resumed cleanly:** the first reprocessing attempt (all 4 tables) was killed intentionally (`TaskStop`) partway through when the required-fields point above was raised, before that fix was made -- left `.datras/parquet/` in a genuinely unknown mixed state, written to memory explicitly rather than assumed safe. Resumed by re-verifying both scripts still parsed, then re-running the corrected script from a clean start (unconditional overwrite, so a mixed starting state self-heals). Full run, all four tables: **2,968 records written, 813 correctly classified as genuinely empty ICES submissions** (`EMPTY_RECORDS`, zero `PARSE_ERROR`) -- LT's count (740) exactly matches its original dedicated backfill from earlier the same day, confirming the generalized script behaves identically for LT. Re-ran the exact schema-inconsistency scan that found the bug: **zero inconsistent columns, all four tables.** Reproduced the user's original failing query against the reprocessed data: no error, both `PlusGr` and `GenSamp` consistently `character`.

Also simplified `archive_06_split_legacy_new.R`'s Step 1 while rebuilding on top of the corrected data: it used to migrate a pre-existing `.datras/parquet/{t}.parquet` (built by some earlier, never-fully-documented process) via a one-time rename, which could never be safely re-run if that file reappeared. Now consolidates directly from the partitioned directory into `.datras/{t}_legacy.parquet` every run (`arrow::open_dataset(part_dir) |> dplyr::collect()`) -- fully known provenance, self-heals if the partitioned data is ever reprocessed again, no "which of two files is current" ambiguity. Re-ran the full split on the corrected data: row counts unchanged from before (145,958 / 13,754,042 / 5,865,076 / 75,310 -- same data, now consistently typed), all four tables' legacy-name ground-truth assertions still pass, and the new-named files' `GeneticSamplingFlag`/`AgePlusGroup` confirmed `string` directly.

---

**Curation pipeline restructured to be legacy-name-primary; icesVocab audit extended to all 190 fields, not just the 52 enum ones (2026-08-09):** Two things converged into one restructure. First, dated proof the pipeline's *file structure* had it backwards: `inst/DATRAS-data-dict.yaml`'s own real-archive verification citations ("verified 2026-07-29") predate the very concept of new-named parquet (invented 2026-08-08/09, this session) by 10 days -- every one of those checks necessarily ran against legacy-named data, yet the yaml stored the *new* name as primary with legacy bolted on as a `details:` annotation, the reverse of how the data actually flows (raw XML → legacy-named parquet → *then* opus's own rename). Second, `data-raw/build_vocab_correction.R` only ever checked the 52 fields already curated as `type: enum` -- the same blind spot principle 9 exists to catch, just one level up: auditing "both names, exhaustively" only helps if the *set of fields checked* is also exhaustive, not pre-filtered by an existing type judgment.

User approved the larger fix over a thin wrapper: `data-raw/spec_01_seed_dict.R`/`spec_02_curate_dict.R` rewritten to key every seed field, correction, `field_specs`/`shared_field_specs` entry, and `col_labels` entry by legacy (real, on-the-wire) name throughout (~140 individual `field=` substitutions, all 1:1 via the real crosswalk, not hand-guessed) -- checked first whether this was even safe: cross-referenced all 40 `shared_field_specs` entries (fields whose spec is meant to apply identically across every table that has them) against real per-table legacy names, and found only **one** case where a table's legacy name for a shared concept actually diverges (`BottomDepth`: HH's own real column is called `Depth`; LT genuinely has both a separate real `Depth` *and* a separate real `BottomDepth`, byte-duplicate columns, Issue 6) -- split into two explicit per-table entries, everything else carried over as a direct 1:1 name swap. `inst/DATRAS-data-dict.yaml` is no longer hand-curated at all: it's now produced by a new final step, `data-raw/spec_03_translate_new_names.R`, a pure rename off the newly-primary `inst/DATRAS-data-dict-legacy.yaml` (spec_02's own output) via a new exported `op_datras_rename_crosswalk()` (`R/field_names.R` -- wraps `op_datras_field_list()`, applying the one known correction, LT's `Depth`/`BottomDepth` exclusion, with a general collision guard rather than a hardcoded special case). `data-raw/spec_03_dict_to_qmd.R` renumbered to `spec_04_dict_to_qmd.R` to keep the filenames in chronological data-flow order (00 crawl → 01 seed → 02 curate → 03 translate → 04 render); it now also renders `inst/DATRAS-data-dict-legacy.yaml` for free, since it walks every yaml under `inst/`. `archive_06_split_legacy_new.R` and `build_vocab_correction.R` both switched from their own independent derivations of the old<->new crosswalk (one by extracting `Legacy field name:` annotations back out of the curated yaml, the other by round-tripping through `op_legacy_field_name()`) to calling `op_datras_rename_crosswalk()`/reading the legacy yaml directly -- one crosswalk source now, not several, which is exactly the class of duplication that caused a real bug earlier this session (a hand-rolled copy of a regex silently drifting from the real function).

**Two real bugs caught by verification, not assumed clean:** (1) `yaml::read_yaml()` silently collapses a single-element YAML sequence back into a bare scalar on read -- `constraints:\n- required` round-trips to a plain `"required"` string, not a length-1 list, breaking write-back for any array-typed field of length 1 (`constraints`, and `examples` wherever a field has exactly one, e.g. LT's `UnitItem`). Only affects `spec_03_translate_new_names.R`, the one script that reads a previously-written curated yaml back in; fixed by re-wrapping length-1 unnamed values before translating (checked length, not `is.list()`/class -- an earlier version of the fix wrongly re-wrapped already-correct length>1 vectors too, caught immediately by the validator turning `Quarter`'s 4-code array into a nested single-element array). (2) A structural diff of the fully-regenerated `inst/DATRAS-data-dict.yaml` against the pre-restructure committed version (every non-`details`/`description` field compared programmatically across all 190 columns, zero mismatches) caught a genuine content loss: LT's `BottomDepth` column lost both its `description` (previously backfilled from HH via the generic same-name shared-field mechanism, which no longer reaches it once the two tables' legacy names for this one field diverge) and its explanatory note about the Issue 6 duplication. Fixed with an explicit, programmatic backfill (not hardcoded text) plus a short cross-reference to LT's own separate `Depth` entry, which already carries the full finding.

**Full audit (`data-raw/build_vocab_field_audit.R`), all 190 fields by legacy name against live icesVocab:** two independently-reported stages, never collapsed into one "the mapping" -- Stage 1 (name-match: does any icesVocab key's prefix-stripped name match this field's legacy name at all? always a guess, per Issue 7) and Stage 2 (value-match: does the matched key's real codes cover the field's real values? only meaningful for already-enum fields; a name match on a non-categorical field is itself the finding, not something to code-compare). Results: 115/190 zero name-match candidates (expected -- mostly genuine measurements); 75/190 with >=1 candidate, 46 of which are the already-known enum fields (byte-identical tally to `build_vocab_correction.R`'s own 46 full/0 partial/6 no-candidate, a useful cross-check that the restructured pipeline didn't silently change anything); the other 29 are fields never typed `enum` that still got a name match -- 16 already independently documented in existing curation notes (`Country`, `Survey`, `StatRec`, `AreaType`, `LTPRP`, `LTSZC`, `PARAM`, `TYPPL`, `SwellHeight`, the last being the already-flagged type-mismatch issue), and **6 distinct fields never checked against icesVocab at all before this sweep**, because nothing had ever prompted checking a field that didn't already look like an enum. Verified each of those 6 directly against the real legacy parquet, not just the name match: `Ship`→`TS_Ship` is a false lead (113 codes, but 88 of 111 real values, 79%, aren't among them -- same shape as the already-known `Survey`/`Country` false leads). The other five are genuine, exact, previously-undocumented matches, zero missing either direction: `Year`→`Year` (62/62 real years), CA's `Maturity`→`TS_Maturity` (47/47), HH's `DepthStratum`→`TS_DepthStratum` (186/186), HH+LT's `Tickler`→`TS_Tickler` (9/9), HL's `CatIdentifier`→`TS_CatIdentifier` (13/13). Filed as Issue 9 in `data-raw/ICES_ISSUE_REPORT.md`. `Tickler` and `CatIdentifier` were flagged here as worth revisiting for enum retyping -- done the same day, see the entry below.

---

**`Tickler`/`CatIdentifier` retyped `enum`; fixed a real undeclared icesVocab package dependency found along the way (2026-08-10):** The two fields flagged just above. Re-verified fresh (not trusted from the earlier sweep) directly against the real legacy parquet: HH's Tickler (9 distinct values) and LT's Tickler (3, `{0,5,8}`, a subset of HH's own) both fully covered by `TS_Tickler`'s 32 codes, zero gaps; HL's CatIdentifier (13 distinct values) fully covered by `TS_CatIdentifier`'s 56 codes, zero gaps. Both vocab keys turned out to be genuine controlled code lists once actually read (`TS_Tickler`: "Zero ticklers"/"One tickler"/etc.; `TS_CatIdentifier`: "1. Level" through "55. Level" plus a `-9`/"Unknown" sentinel) -- not just names that happened to match. `shared_field_specs`'s `Tickler` entry and `field_specs`'s `CatIdentifier` entry rewritten to `type = "enum"` with the full vocab code list as `values` (not just the observed subset, matching `Gear`'s own existing convention), dropping their previous `range`/`examples`, which `apply_col_update()` never sets alongside `type: enum` for any other field either.

Left `Year`, `Maturity`, and `DepthStratum` (the other three real matches from the same sweep) exactly as they were, each for a distinct, already-documented reason, not a shared cutoff: `Year` is a continuously-advancing ordinal (next year needs a code that doesn't exist yet), not a fixed closed set; `Maturity`'s own field description says its coding depends on the sibling `MaturityScale` field, so `TS_Maturity`'s 63 codes are plausibly a union across several distinct scales, not one flat list; `DepthStratum`'s own description says strata are survey-specific, in tension with treating one vocab key as universal for it. Recorded this reasoning in `data-raw/ICES_ISSUE_REPORT.md`'s Issue 9 too, since leaving it unstated there would have made the issue report's own findings look inconsistent with what opus's shipped spec actually does for two of the five.

Found and fixed a real, unrelated bug while building the generic version of this: the pre-existing `Gear` enum (`shared_field_specs`, `get_gear_enum_values()`) called `library(icesVocab); getCodeList('Gear')` directly -- the *real* external `icesVocab` package, not opus's own `op_vocab_get_codes()` -- despite `icesVocab` appearing in neither `Imports` nor `Suggests` in `DESCRIPTION`. Would have failed this script on any machine without that package installed, quietly contradicting opus's own "zero R-package dependency on icesVocab" claim (AGENTS.md, above). Confirmed `op_vocab_get_codes("Gear")` returns byte-identical output (99 codes) before generalizing `get_gear_enum_values()` into `get_vocab_enum_values(key)`, used for `Gear` (unchanged behavior) and the two new enum fields alike.

Verified: structural diff of the regenerated yaml against the pre-change committed version shows exactly the 3 intended changes (`HH.Tickler`, `LT.Tickler`, `HL.SpeciesCategory` -- the curated name for legacy `CatIdentifier` -- gain `type: enum` + their vocab's full `values`, lose `range`/`examples`) and nothing else. `build_vocab_correction.R`'s enum-field tally moved from 52/46-full to 55/49-full, exactly the expected +3; `build_vocab_field_audit.R`'s "non-enum field with a name match" bucket moved from 29 to 26, the same 3 fields crossing over. `devtools::check()` clean (0/0/1, the usual `TESTO.R` NOTE).

---

**Data-dict feature adoption, a real archive-integrity bug, and a documentation-architecture change (2026-08-16):** Started as a review of what new data-dict features (`relationships`, `todo`, `definitions`, assertions, the JSON/HTML `report` format, `render`) opus wasn't yet using; turned into the single most consequential session since the 2026-08-08/09 type-casting fix.

**`relationships` implemented.** HH's 8-field composite key (`Survey`/`Year`/`Quarter`/`Country`/`Platform`/`Gear`/`StationName`/`HaulNumber`) marked `primary_key`; HL/CA/LT's matching columns `required, foreign_key`; three joins declared (`data-raw/spec_02_curate_dict.R`'s new `relationships` block, translated to curated names by new logic in `spec_03_translate_new_names.R` -- a join's `table.column` tokens, translated per-table via the same crosswalk the column rename already uses). Collapses four copies of the same prose (previously hand-written once per table's own `details`) into one structurally-checked fact. Caveat, confirmed by actually testing it against the real archive: `validate-data`'s `D05` only checks single-column foreign keys, so every column's `D05` step reports `unevaluated` for this composite key -- spec-level correctness only, no automatic cross-table orphan detection.

**A real, systemic archive-integrity bug, found via that test.** Testing the relationships surfaced a contradiction: `known-issues.yaml`'s `ca_haulno_unlinkable_to_hh` says CA's `HaulNo` carries a `-9` sentinel, "0 true nulls" -- but `.datras/CA.parquet` showed 288,581 true nulls, zero `-9`. Root cause: `archive_04_parse_phase2.R`/`archive_05_backfill_lt_partitions.R` each carried a `GLOBAL_SENTINELS` list (`-9, -99, -999, -1, -5, -95, -100, -900, 88888888`) applying a blanket, unconditional sentinel-to-`NA` conversion across every column, before type casting -- introduced between the pipeline's original 2026-08-05 commit and the 2026-08-08 reorg (confirmed via git history), i.e. *after* the `-9` finding above had already been verified as real, non-null data on 2026-08-07. Confirmed directly against raw XML: `CA_BTS_2004_Q3` has 513 literal `<HaulNo>-9</HaulNo>` tags, all silently becoming `NA`.

Not confined to one field: a full re-scan for all 9 sentinel values across every numeric column, once recoverable, found 101 (table, column, sentinel) combinations with real counts. Worst: `HH.Tickler` -- icesVocab's own documented code for `-9` is *"No ticklers are allowed"*, a real answer, not an absence of one -- was `-9` for 113,645 of 145,958 rows (78%), the modal value for the entire column. This directly undermines the 2026-08-10 Tickler/CatIdentifier "zero gaps" finding above: that check ran against an already-silently-scrubbed archive, so it could not have seen this even if it had looked.

Fix: removed `GLOBAL_SENTINELS`/`replace_sentinels()` entirely from both scripts (same reasoning already established for enum-detection in `archive_00_wsdl_types.R`: sentinel *meaning* is a curation conclusion reached by looking at real data, not a type-casting decision, and it's circular to bake it into this stage -- you can't discover a code is meaningful from data this stage already erased). Validated on the single known file first, then reprocessed the full archive from raw XML for all four tables (`Success: 2968 | Error: 813`, identical to the already-documented 2026-08-08/09 run; all 813 confirmed still `EMPTY_RECORDS`, no new failure mode). `op_flag_violations()` re-confirmed the original `ca_haulno_unlinkable_to_hh` finding exactly (288,581 `HaulNumber:D04_range`, now correctly `-9` not null). Full writeup, including the quantity-vs-ordinal/id/enum triage for telling "standard missing" fields apart from "field-specific meaning" ones, and the `WindSpeed`-as-co-parameter technique used to settle ambiguous cases: `vignettes/articles/technical-notes.md` section 4.

**Six field-level spec fixes followed, each root-caused against the rebuilt archive, not guessed:**
- `HH.SwellHeight`/`LT.SwellHeight`: both were typed wrong at some point (LT still `enum`, matching the vocab's H/L/M/N/NR/VH codes neither table's real data actually uses -- confirmed exhaustively: no second, differently-named icesVocab key exists either, all 580 registered keys checked). Retyped `number(quantity)`. Range settled at `[0, 13]` for HH using `WindSpeed` at the same haul as an independent check (median wind rises smoothly with swell height through 13m for values kept, then breaks down implausibly for values excluded as decimal-point errors -- a real discontinuity, not an arbitrary cutoff). `LT.SwellHeight` turned out to be a byte-for-byte copy of HH's own value (55,914/55,914 matched non-null rows identical) and now inherits HH's range/reasoning entirely rather than being independently re-derived.
- `CA.Age`: `-9` (1,995,814 rows, 34%) was entirely undocumented, dwarfing the already-known `-1`/`-5`/`-95` cluster (1,759 rows combined).
- `DateofCalculation` (HH/HL/CA): `-9` (5.76-7.93% depending on table) already excluded by the declared range, just never named, unlike this file's own house style elsewhere.
- `Quarter`/`Tickler`/`CatIdentifier`: confirmed live WSDL declares all three `int`, not string -- an accepted, permanent `M01` divergence between the WSDL-driven archive and these curated `enum` specs, same class as the already-accepted `Valid_Aphia` pattern, documented as such rather than "fixed" by changing either side.

**Checked whether other "shared" fields (beyond the composite key) are also literal HH copies** -- ~30 of them are (100% or near-100%), confirming a broader pattern than just `SwellHeight`. Three real exceptions found and documented instead of forced to match: `CA.GearEx` (92.27% -- CA supplements HH's generic `-9` with a real code in the other 7.73%, never contradicts it), `LT.Rigging` (95.33%, a near-symmetric `FB`/`FW` swap, independent recording variance not a one-directional error), `LT.HaulVal` (99.97%, negligible). Also diagnosed the 2,026-row (2.7%) LT-to-HH composite-key match residual: 2,010 of them are `Survey=BTS, Year=2025, Quarter=1`, where HH's own submission is genuinely empty (matches the already-known `EMPTY_RECORDS` log entry for that exact cell) -- an ICES-side submission gap, not an opus or LT data-quality problem. New `known-issues.yaml` entry (`lt_bts_2025_q1_orphaned`); the remaining 16 (`Survey=NS-IBTS`: 10 at Year=2026/Quarter=1, 6 at Year=2025/Quarter=3) later diagnosed as a *different* cause from the BTS gap -- HH has abundant real data for both exact cells (357 and 331 rows respectively), so it isn't a missing-submission gap, just a small residual of the same open-cause kind as `ca_haulno_tail_mismatch` below.

**`todo`/`definitions` adopted for the first time.** Neither `apply_col_update()`/`apply_table_update()` (`spec_02_curate_dict.R`) supported either field -- added. Six `todo:` entries convert existing "flagged for expert review, not investigated further"-style prose into structured, `validate-spec`-visible (S31) notes (`HL.LngtClass`, `CA.Age`, `LT.PARAM`, `HH.TowDir`, and CA's own 700-row tail-mismatch residual); a seventh candidate (LT's own HaulNumber gap question) was answerable outright (checked: zero nulls, zero `-9`) so got resolved in prose instead of left open. One `definitions` entry: `CA.linkable_to_hh` (`HaulNo != -9`), formalizing the already-extensively-documented orphan filter as a reusable, translatable expression. Both features needed the same "written in the legacy-keyed pipeline, so use legacy names, and `spec_03` must translate the expression" fix that `relationships` already required -- caught immediately by `validate-spec`'s own S20 check both times.

**`op_flag_violations()` reconsidered, not simply deprecated.** Checked whether the CLI's report row-cap (5 per problem) is configurable -- it isn't, confirmed via `--help` and the binary's own embedded strings. So `op_flag_violations()`'s exhaustive per-row marking genuinely can't be replaced by the report format; its docstring's old justification ("CLI doesn't report row numbers") was simply false and is now corrected to explain the real reason it still exists. Added `op_validate_spec(json = TRUE)` (it was the one validate-level not exposing the CLI's structured report; `op_validate_meta()`/`op_validate_data()` already had it via `$result`) and `op_validation_problems()` (pure reshaping: flattens any report's `problems` into a data frame, zero new logic). 21 exported functions now, not 17 -- see Implementation section above, also corrected.

**Documentation architecture decided: `render`, not the hand-rolled Quarto generator.** `data-raw/spec_04_dict_to_qmd.R` and its three generated `.qmd` files removed; `render` (already wrapped as `op_render_spec()`) does everything the generator did plus a relationship diagram, search, and live data profiling, none of which the generator ever had (it also never rendered `relationships` at all). `known-issues.yaml` gets no replacement page -- confirmed `render` rejects it outright (`Unknown property 'sentinels'`), since it was never a real data-dict.yaml document, just the same file extension. Decision: `render` stays an on-demand tool, nothing committed -- `.datras/`'s full archive is local/gitignored and continuously growing, so a page profiled against it can't be a "kept in sync" repo artifact, only a point-in-time export generated and shared when useful. `op_render_spec()` gained a `data_dir` parameter for exactly that (text-substitution based, not a `yaml::read_yaml()`/`write_yaml()` round-trip, deliberately avoiding the singleton-array bug `rewrap_singleton_arrays()` exists to work around). `docs/` (confirmed gitignored, not actually git-tracked despite some stale history) wiped and rebuilt clean via `pkgdown::build_site()` rather than hand-picking individual stale files -- it held leftovers from several past naming eras (an `icesvocab`-named generation, a `-formatted` one) beyond what had even been scoped for removal.

**Still open:** `data-raw/ICES_ISSUE_REPORT.md` still not sent (longest-standing item in this project) -- now including Issue 10 (`swell_height_vocab_unused`: icesVocab's own single `SwellHeight` code list used by neither HH's nor LT's real submissions) and a non-bug "Suggestion for consideration" section (the `.id`-surrogate-key idea: concatenating the composite key into one field, so HL/CA/LT could drop the repeated columns -- correctly not built locally, since ICES's own current format doesn't have it and opus can't retrofit one without diverging from what ICES actually publishes, but worth raising as something for ICES to consider). `inst/*.parquet`'s own staleness relative to some corrections (e.g. `DateofCalculation`) deliberately deferred, provenance unclear (predates the `archive_0N` pipeline) -- **explicitly undecided, not just deferred**: until settled, the process for yaml/issues/documentation must rely only on `.datras/*.parquet`, never `inst/*.parquet` (a draft resampling script, `data-raw/archive_07_build_test_samples.R`, exists on disk, untracked, unused, pending this decision). The ~26 enum fields declaring `-9` as a curated code were all individually checked against icesVocab/raw data (not left unexamined): ~20 confirmed as the standard generic-missing convention, one genuine exception found (`DataType`'s `-9` = icesVocab's specific "Invalid hauls", not generic missingness), and 5 remain genuinely unresolved (`HL.DevStage`, `HL.LenMeasType`, `CA.PlusGr`, `HL.LngtCode`, `CA.LngtCode` -- each has no icesVocab key resolving either way, tracked as `todo:` in `spec_02_curate_dict.R`, S31-visible). `TODO.md` updated to match this entry.

**Follow-through session, same day (2026-08-16):** Fixed two real, independent bugs in `.validate_via_dict()` (`R/validation.R`, shared by `op_validate_meta()`/`op_validate_data()`) that had silently made both non-functional against the shipped dictionary. (1) The table end-boundary scan looked only for the next `- name:` line, so the *last* table in the file (LT) ran off the end and swallowed `relationships:`/`glossary:` into its reconstructed temp YAML -- fixed by stopping at any unindented (column-0) line, which correctly covers both the next table and any bare top-level key. (2) Once (1) was fixed, a second, previously-masked bug surfaced: removing an existing `source:` block before injecting a new one dropped only the `source:` line itself, not its indented `parquet:` child line below it, orphaning that child and producing invalid YAML for *every* table, not just the last -- fixed by walking forward while lines stay nested (>= 3 leading spaces). Verified against all four tables for both `validate-meta` and `validate-data`: both now run cleanly (no parse errors) and return real, substantive results -- 3 `M01` type mismatches for HH (`Quarter`, `Month`, `Tickler`), all now documented as accepted WSDL-vs-enum divergences (this pass also found and closed one genuine gap: `Month` had the identical divergence as `Quarter`/`Tickler` but no `details:` note of its own -- added, matching the established pattern, verified against the live WSDL directly rather than assumed); `Valid_Aphia` (HL/CA) and LT's `DateofCalculation` type mismatches were already tracked via existing `issue_id`s, not new. 11 `S31` unresolved-`todo` warnings reported, matching this session's own deliberately-added open questions exactly. `known-issues.yaml`'s `lt_bts_2025_q1_orphaned` entry updated with the NS-IBTS diagnosis (see above). E1 (data-dict CLI's `skill-read`/`skill-create`) investigated: both are generic, dataset-agnostic Claude-Code-style skill prompts (YAML frontmatter + markdown instructions) for reading/authoring *any* data-dict.yaml, not opus-specific data or features -- `skill-read` is retrievable on demand (`data-dict skill-read`) by any future consumer of opus's shipped YAML, no need to vendor a copy that could drift from the actively-developed (pre-1.0) CLI; `skill-create`'s interview-driven process doesn't match opus's existing evidence-driven bootstrap pipeline (`spec_01`/`02`/`03`) and isn't a fit to adopt. No action taken on either, by design.

**Pre-staging review, same day (2026-08-16):** Before committing the day's accumulated changes, ran a three-part review at the user's request: (1) compared opus's shipped YAML against data-dict's own canonical worked examples (`~/garbage/data-dict/site/examples/*.yaml`); (2) audited opus's dictionary against the data-dict CLI's own `skill-read`/`skill-create` prompts as a rubric; (3) a from-scratch structural review of the whole repo, verifying against real files/data rather than this file's own narrative. Two fixes landed as a direct result of (1)/(2) (below); everything else is recorded as an open backlog rather than silently fixed, per "report findings before acting."

**Fixed: `AgeSource`/`AgePrepMet` were resolving to redirect-only icesVocab keys.** `TS_AgeSource`/`TS_AgePrepMet` (the name-matched keys `ICES_ISSUE_REPORT.md`'s Issue 8 table listed as "full match") turn out to be non-authoritative: each one's own icesVocab `Description` is a bare "see X" cross-reference (`see SampleType`, `see PreparationMethod`) to a different, non-`TS_` domain -- invisible to name-matching, since the redirect target shares no name with the field. Real CA archive data has zero code overlap with either `TS_` key but matches `SampleType`/`PreparationMethod` exactly, code-for-code (including a literal lowercase `otolith` code in `SampleType`, not just its description). Fixed in `spec_02_curate_dict.R` (moved from a mis-targeted `col_labels` entry -- see next paragraph -- into a proper `field_specs` entry pointing at the redirect targets); `build_vocab_correction.R` gained an explicit two-entry override list for these known redirects, and `inst/DATRAS-vocab-correction.csv`/`op_vocab_resolve_datras_key()` regenerated to match. `ICES_ISSUE_REPORT.md`'s Issue 8 gained an addendum describing the redirect-without-structure problem generally (`TS_Ship`->`SHIPC` and `TS_Country`->`ISO_3166` carry the same pattern but don't affect opus, since `Ship`/`Country` are typed `string`, not `enum`) and corrected its own table's two wrong "full match" rows.

**A real bug caught mid-fix, worth remembering.** The first attempt at this fix silently did nothing -- `type`/`values`/`details` all appeared to apply with no error, but the shipped YAML never changed. Root cause: the original `AgeSource`/`AgePrepMet` entries lived in `col_labels`, a list meant only for adding a human-readable `label` to terse column names; its apply loop (`for (spec in col_labels) curated <- apply_col_update(curated, spec$table, spec$field, list(label = spec$label))`) hardcodes the update to `label` alone, silently discarding every other field on the list entry with no warning. Anything ever added to a `col_labels` entry beyond `label` is dead on arrival. Fixed by moving the entry to `field_specs` (which forwards every field via `spec[setdiff(names(spec), c("table","field"))]`). Whether `col_labels`'s loop should instead reject unexpected keys outright, so this fails loudly next time, is flagged below, not done here.

**Fixed: three CA sampling-flag columns (`GenSamp`/`StomSamp`/`ParSamp`) had the same stale "100% clean" claim already fixed for Tickler/CatIdentifier/Age/SwellHeight earlier today, just missed by that pass.** Each was documented (2026-07-29) as a clean two-value Y/N split, zero `-9` -- but that check ran against obus's own icesDatras-fetched archive, whose `parseDatras(fix_types=TRUE)` unconditionally scrubs `-9` before opus ever saw the field (same caveat this file's header already documents for every other pre-2026-08-05 field-level stat). Re-verified 2026-08-16 against `.datras/CA.parquet`: `-9` is now the dominant value in all three (82.9-85.6% of CA). `values:`/`details:` updated to match, same pattern as the earlier four fields.

**Still open (not fixed, tracked here and in TODO.md):**
- A new, likely-serious archive-integrity bug: "T"/"F" values in several enum columns (`HL.SpeciesCodeType`, `HH`/`HL`/`CA.DoorType`, `HH.Rigging`, `CA.IndividualSex`) are sometimes stored as the literal text `"TRUE"`/`"FALSE"` instead of `"T"`/`"F"` -- confirmed directly against `.datras/*.parquet` (1,536,333 of 13,754,042 `HL.SpeciesCodeType` rows alone), hits HH/HL/CA, never LT. Same class of problem as `sentinel_replacement_data_loss`; needs its own root-cause-and-rebuild cycle, not a bolt-on fix.
- 6 dead `R/` files (`op_apply_curated_spec.R`, `op_audit_yaml_phase2_mismatch.R`, `op_build_final_yaml.R`, `op_check_type_mismatch.R`, `op_enrich_stage2_yaml.R`, `op_minimal_yaml.R`) implementing a Phase-1/2/3 bootstrap workflow superseded by `spec_00`-`03` since 2026-08-05/08 -- zero real callers, but each still `@export`-tagged (only kept out of the real API because `NAMESPACE` is hand-frozen, not roxygen-managed) with a generated man page. This file's own Implementation section (above) still describes them as live, immediately above the paragraph describing the real pipeline.
- `vignettes/articles/using-opus.qmd` (untracked, never committed) is severely stale: describes a "descriptive vs. strict icesVocab-only" two-YAML scheme that no longer exists, its known-issues.yaml walkthrough uses a schema that doesn't match the real one (`issues$issues`/`title,status,description` vs. the real `known_violations`/`id,severity,field,table,issue,extent,implication,discovered`), asserts the CLI's `rows` field is always empty (already confirmed false this session), cites a wrong GitHub org for data-dict (real remote: `tidyverse/data-dict`). Needs a rewrite-or-drop decision.
- `inst/DATRAS-data-dict-legacy.yaml`'s `source:` stanzas point to `{TABLE}_legacy.parquet` files that don't exist in `inst/` (only in gitignored `.datras/`).
- Descriptions/details have hard `\n` line breaks baked into the actual string value, not just YAML source formatting -- `format_long_text()` in `spec_02_curate_dict.R` manually word-wraps and joins with `\n`; confirmed via `yaml::read_yaml()` that the parsed value itself contains mid-sentence breaks. data-dict's own canonical examples use folded (`>`) scalars instead, which round-trip clean; opus uses `>` zero times.
- `relationships`' `conflicts` field is never used, despite real cases (HH and LT share 34 non-key columns; `RecordHeader`'s enum meaning genuinely differs per table -- HH: "Haul Information", HL: "Length Frequency Distribution").
- A literal `<U+2192>` escape (not a real arrow character) in `origin:` (both YAMLs, line 10) -- confirmed via hexdump: the bytes are the literal ASCII text, not UTF-8.
- 6 of 14 tests in `tests/testthat/test_validation.R` always silently skip (`skip_if_not(file.exists("HH.parquet"), ...)` checks a bare relative filename, never `system.file(...)`) -- the exact functions fixed twice today have zero real test coverage right now.
- `DESCRIPTION` says "Data-only package (no computational functions)" against its own 21 exported functions and its own computational `Imports`/`Suggests` (arrow, dplyr, httr2, XML, ...).
- `HH.StartTime`'s details cites "same as StatRec above" -- the real curated column is `StatisticalRectangle`, defined *after* StartTime, not above; same bug in the legacy YAML (predates the rename step).
- Glossary gaps: `icesVocab` (35 uses), `WSDL` (33 uses, plus operation names like `getHHdata`), `WoRMS`/`AphiaID` (12 uses each), internal rule codes `M01`/`S24`/`D01`/`D04` (cited repeatedly as justification, never explained), `CPUE`, `OSPAR`, `SeaDataNet` -- all used, never defined (`MSFD`/`EEZ`/`NM` are correctly spelled out inline, so this isn't a blanket problem).
- Minor: superfluous trailing `.0` on ~158 `range` values (R double-precision leaking into the emitter); 8 table/dataset-level `description`/`details` entries use flow-scalar style unlike all 190 column-level entries; `HL.SubsamplingFactor`'s cited row count (14,256,091) is ~3.5% off the current archive (13,754,042) -- a spot-check, not an exhaustive one, so others may have similarly drifted since the rebuild; two tiny case mismatches (`HH.ThermoCline` 3 rows, `LT.LTSRC` 1 row); `scripts/build-dictionary.sh` (gitignored) references a script deleted 2026-08-08.
- `col_labels`'s silent-field-drop hazard (see above) -- consider making its apply loop reject unexpected keys instead of silently ignoring them.

Found the real provenance of `inst/*.parquet` along the way: `data-raw/GENERATE_test_data_ns_ibts_2026q1.R` (2026-07-31) -- a narrow single-survey/quarter (NS-IBTS/2026/Q1) extract from **obus's** local files, not a broad sample. Corrects `archive_07_build_test_samples.R`'s own header claim ("no script anywhere in this repo previously built these files") -- that claim was wrong; the D1 decision (whether/how to replace `inst/*.parquet`) remains open regardless.

**Session-close correction, same day (2026-08-16):** A post-commit status check (walking back through every item in the pre-staging review above) found three things that were investigated and verbally reported but never actually written into this file or `TODO.md`, so they'd have been silently lost otherwise: the `swell_height_type_mismatch` stale "not yet filed" cross-reference (verified still present, both HH and LT), the Quarto-generator-reinvention process lesson (said "worth a line," never added one), and `archive_07_build_test_samples.R`'s own false provenance claim (noted as wrong in this file's own text above, never fixed in the script itself). All three re-added to `TODO.md`'s backlog. Lesson for future close-outs: a verbal "still open, tracked" claim needs to be checked against the actual backlog file before trusting it, not just against memory of having said it.
