% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive.R
\name{op_rename}
\alias{op_rename}
\title{Rename between the legacy and current naming schemes}
\usage{
op_rename(d, table, to = c("legacy", "current"), path = op_archive())
}
\arguments{
\item{d}{A data frame or lazy `tbl`.}

\item{table}{One of `"HH"`, `"HL"`, `"CA"`, `"LT"`.}

\item{to}{`"legacy"` for ICES's on-the-wire names, `"current"` for opus's.}

\item{path}{Archive root; see [op_archive()].}
}
\value{
`d` with its columns renamed. Unmatched columns are left alone.
}
\description{
Rename between the legacy and current naming schemes
}
\examples{
\dontrun{
hh <- op_con("HH") |> head(3) |> dplyr::collect()
op_rename(hh, "HH", to = "legacy")   # Platform -> Ship, HaulNumber -> HaulNo
}
}
