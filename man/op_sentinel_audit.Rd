% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sentinels.R
\name{op_sentinel_audit}
\alias{op_sentinel_audit}
\title{Count sentinel values per column in a parquet file}
\usage{
op_sentinel_audit(path, table, sentinel = NULL)
}
\arguments{
\item{path}{Path or URL to a parquet file.}

\item{table}{Character scalar: `"HH"`, `"HL"`, `"CA"` or `"LT"`; used to
attach each column's resolved policy.}

\item{sentinel}{The sentinel value, as a string. Defaults to the
registry's global value.}
}
\value{
Data frame, one row per column that contains the sentinel:
  `field`, `n` (sentinel count), `n_rows`, `pct`, `n_null`, `action`,
  `label`. Sorted by `pct` descending.
}
\description{
The evidence sweep behind every strip/keep decision, and the regression
check after a rebuild: a column must either lose all its sentinels (and
gain exactly that many nulls) or keep every one of them. Anything in
between is a bug.
}
\examples{
\dontrun{
  op_sentinel_audit(".datras/to_https/HH.parquet", "HH")
}
}
