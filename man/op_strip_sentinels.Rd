% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sentinels.R
\name{op_strip_sentinels}
\alias{op_strip_sentinels}
\title{Replace sentinels with NA, where they mean absence}
\usage{
op_strip_sentinels(df, table, sentinel = NULL, quiet = TRUE)
}
\arguments{
\item{df}{Data frame carrying current field names.}

\item{table}{Character scalar: `"HH"`, `"HL"`, `"CA"` or `"LT"`.}

\item{sentinel}{The sentinel value, as a string. Defaults to the
registry's global value.}

\item{quiet}{Logical; if `FALSE`, report what was stripped and kept.}
}
\value{
`df` with absence-meaning sentinels replaced by `NA`. Columns
  resolved to `keep` are untouched.
}
\description{
Phase B of the conversion. Runs **after** [op_rename_to_new()] -- the
registry and the dictionary are both keyed by current field names -- and
**before** [op_cast_to_spec()], because a `Date` column cannot hold `-9`.
}
\examples{
\dontrun{
  df <- op_strip_sentinels(df, "HH")
}
}
\seealso{
[op_sentinel_policy()] for why each column is treated as it is.
}
